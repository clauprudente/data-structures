//ananda mariana
//resp> AANA
//


function LCS (s1, s2, idx1, idx2, let memo = new Map() ){

    

    if(idx1 >= s1.length || idx2 >= s2.length) return 0
    if(memo.has(idx1, idx2)) return memo[(idx1, idx2)]

    if(s1[idx] == s2[idx2]){
        memo
    }

}